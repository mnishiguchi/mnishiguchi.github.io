{
  "name": "mnishiguchi-jekyll",
  "private": true,
  "scripts": {
    "develop": "jekyll clean && jekyll serve --watch --incremental",
    "build": "JEKYLL_ENV=production jekyll clean && jekyll build",
    "cp-bootstrap-scss": "mkdir -p ./_sass/bootstrap && cp -r ./node_modules/bootstrap/scss/ ./_sass/bootstrap/"
  },
  "dependencies": {
    "bootstrap": "^4.4.1"
  }
}
